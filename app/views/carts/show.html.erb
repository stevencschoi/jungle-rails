<section class="cart-show">

  <header class="page-header">
    <h1>My Cart</h1>
  </header>

  # conditionally render cart based on existence of items
  <% if cart.size > 0 %>
    <%= render 'cart' %>
  <% else %>
    <%= render 'empty' %>
  <% end %>

</section>
